What happens when you initialize a repository? Why do you need to do it?

	When a repository is initialized, a git file is added to the directory. This needs to be done so that git can recognize it.

How is the staging area different from the working directory and the repository? What value do you think it offers?

	The staging area is different in that it is the work space before committing something. It allows the user to move files around before actually committing.

How can you use the staging area to make sure you have one commit per logical change?

	You can use the staging area to move only the files that you made the changes before committing them.

What are some situations when branches would be helpful in keeping your history organized? How would branches help?

	Branches help keep history organized because different versions are saved in different areas.

How do the diagrams help you visualize the branch structure?

	Diagrams help me visualize the branch structure because it begins to look like a road map, which to easy to follow.

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?

	The result is a final product of both branches combined. It is represneted that way because it shows 2 branches becoming 1.

What are the pros and cons of Git’s automatic merging vs. always doing merges manually?

	Pros of Git's automatic merging is that it saves lots of time for manually merging files. Cons are that sometimes code may get merged that you don't want.